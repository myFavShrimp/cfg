[[default]]
names = [
  ".config/helix",
  ".config/alacritty",
]
# prevent dotlink from trying to link to ~/.config/.config/
to = "~/.config/.."

[[linux]]
names = [
  ".config/nushell",
]
# prevent dotlink from trying to link to ~/.config/.config/
to = "~/.config/.."
